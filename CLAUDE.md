# od-sifra — Claude Code instructions

## Design constraints

- **App background must be black — either `#000000` or `#0A0A0A`.** This applies to both light and dark modes in `web/src/app/globals.css` (`--background` in `:root` and in `.dark`). Any other value requires asking before changing.
